{
  "name": "backend",
  "version": "1.0.0",
  "description": "This Cloudflare Worker proxies requests to OpenRouter's API, keeping your API key secure on the server side. Users don't need to enter their own API key.",
  "main": "worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
