name = "writer-app-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# GOOGLE_API_KEY is stored as a Cloudflare secret (wrangler secret put GOOGLE_API_KEY)

# Uncomment to use a custom domain
# routes = [
#   { pattern = "api.yourapp.com/*", zone_name = "yourapp.com" }
# ]
