/**
 * Utils barrel export.
 *
 * Currently empty -- utilities will be added here as the desktop app
 * grows (e.g., date formatting, content converters, debounce helpers).
 *
 * Usage:
 *   import { someUtil } from '@/utils';
 */

// Placeholder -- add utility exports here as they are created.
// Example:
//   export { formatDate } from './formatDate';
//   export { debounce } from './debounce';
